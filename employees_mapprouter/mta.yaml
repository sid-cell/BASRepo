_schema-version: "3.2"
ID: employees_mapprouter
description: Employees Managed Approuter
version: 0.0.1
modules:
- name: employees_mapprouter-router
  type: approuter.nodejs
  path: router
  requires:
  - name: employees_mapprouter-html5-repo-runtime
  - name: employees_mapprouter-uaa
  - name: employees_mapprouter-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    memory: 256M
resources:
- name: employees_mapprouter-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: employees_mapprouter-${space-guid}
    service: xsuaa
    service-plan: application
- name: employees_mapprouter-html5-repo-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: employees_mapprouter-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: employees_mapprouter-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
