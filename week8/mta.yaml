_schema-version: "3.2"
ID: week8
version: 0.0.1
modules:
- name: week8-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: week8-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ques6.zip
      name: ques6
      target-path: resources/
- name: ques6
  type: html5
  path: ques6
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: week8-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: week8-html5-srv
    service-plan: app-host
- name: week8-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: week8-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
