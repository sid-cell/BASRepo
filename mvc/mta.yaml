_schema-version: "3.2"
ID: mvc
version: 0.0.1
modules:
- name: mvc-approuter
  type: approuter.nodejs
  path: mvc-approuter
  requires:
  - name: mvc_html_repo_runtime
  - name: mvc-dest-srv
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    memory: 256M
- name: mvc_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: mvc_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - week9.zip
      name: week9
      target-path: resources/
- name: week9
  type: html5
  path: week9
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: mvc_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: mvc_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: mvc-dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: mvc-dest-srv
    service-plan: lite
parameters:
  deploy_mode: html5-repo
